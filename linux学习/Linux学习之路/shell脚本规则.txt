1、shell脚本一般开头指明所使用的shell,如#!/bin/bash
2、shell脚本变量
	1>变量使用及定义(不需要c语言变量那样,只需要直接使用即可),访问时加入$PATH
	2>变量=两边不能有空格
	3>变量在脚本中都是以字符串存储
	
3、shell脚本参数
	1> $1、$2... 传递给shell的具体参数
	2> $0 shell脚本名称
	3> $# 传递给shell的参数个数
	4> $* 包含所有的脚本参数,并且以IFS变量规定的符号分割
	5> $@ 如$*，但是不依赖IFS变量分割
	6> $$ shell脚本进程号